"""SQLAlchemy ORM models and database session management (database.py,
tables.py). Not the project's models/ dir at the repo root, which holds
joblib artifacts. Pydantic request/response schemas, if needed later, go
in a separate module -- keep them out of here to avoid confusing the two."""
