PORT=8000
COMMAND_SERVICE_URL=http://localhost:8001
QUERY_SERVICE_URL=http://localhost:8002
# Stub auth: when true, requests must carry a non-empty bearer token. Real auth
# is a backlog feature (architecture Section 27).
AUTH_ENABLED=false
# Outbound timeout to backend services (seconds).
UPSTREAM_TIMEOUT_SECONDS=10
